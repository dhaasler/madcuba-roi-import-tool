
x_fits = 233;   // starts at 1,1 on the bottom-left
y_fits = 211;
x_imagej = 232; // starts at 0,0 on the top-left
y_imagej = 350;
// makePoint(x_imagej, y_imagej);   // malo
// makePoint(x_fits, y_fits);
makePolygon(x_imagej, y_imagej);    // less than 3 points
// makePolygon(x_fits, y_fits);     // less than 3 points